import { DirBtnDirective } from './dir-btn.directive';

describe('DirBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new DirBtnDirective();
    expect(directive).toBeTruthy();
  });
});
